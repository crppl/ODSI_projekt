(notka dla siebie na przyszłość)
W celu włączenia aplikacji należy zbudować i postawić kontener z obrazu powstałego z `docker-compose.yaml`. W katalogu z tym plikiem należy wywołać:
`docker-compose build`
`docker-compose up`

link do repozytorium github: https://github.com/crppl/ODSI_projekt/